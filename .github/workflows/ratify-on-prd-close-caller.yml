name: Ratify on PRD close

"on":
  issues:
    types: [closed]

jobs:
  ratify-on-prd-close:
    permissions:
      contents: write
      issues: read
    uses: collod873/claude-workflow/.github/workflows/ratify-on-prd-close.yml@canary/tracer
    with:
      runner: ubuntu-latest
      machine_ref: canary/tracer
