name: Shape — accept

"on":
  issues:
    types: [labeled]

jobs:
  accept:
    permissions:
      contents: write
      issues: write
    uses: collod873/claude-workflow/.github/workflows/shape-accept.yml@canary/tracer
    with:
      runner: ubuntu-latest
      machine_ref: canary/tracer
