name: Shape

"on":
  issues:
    types: [labeled]
  issue_comment:
    types: [created]

jobs:
  shape:
    permissions:
      contents: read
      issues: write
    uses: collod873/claude-workflow/.github/workflows/shape.yml@canary/tracer
    with:
      runner: ubuntu-latest
      machine_ref: canary/tracer
    secrets: inherit
