name: Verify

"on":
  push:
    branches: [main]
    paths-ignore:
      - "**.md"
      - "docs/**"
      - "LICENSE"
  repository_dispatch:
    types: [implementation-opened]

jobs:
  verify:
    permissions:
      contents: write
      pull-requests: read
    uses: collod873/claude-workflow/.github/workflows/verify.yml@canary/tracer
    with:
      runner: ubuntu-latest
      machine_ref: canary/tracer
