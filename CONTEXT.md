The seed is one package.json, one lockfile, one contract and one test: the smallest
target the machine can check that is provably not itself. A slice here names a path
rooted at the repository root, because the lanes that read it never share a working
directory.
